service:
  name: ${env:SLS_SERVICE_NAME}

provider:
  name: aws
  runtime: nodejs12.x
  stage: ${self:custom.stage}
  region: ${env:REGION}
  stackName: ${env:SLS_SERVICE_NAME}Stack
  endpointType: regional
  role: ${env:ACCESS_ROLE}
  httpApi:
    name: ${env:HTTPAPI_NAME}
    payload: '2.0'
  iamRoleStatements:
    - Effect: Allow
      Action:
        - s3.ListObjects
        - s3:GetObject
      Resource:
        - arn:aws:s3:${env:CDN_BUCKET_NAME}

functions:
  serveRootHandler:
    name: ${env:SLS_SERVICE_NAME}-serveRootHandler
    handler: v1/handlers.servePageHandler
    events:
      - httpApi:
          path: /
          method: GET
  serveProxyHandler:
    name: ${env:SLS_SERVICE_NAME}-serveProxyHandler
    handler: v1/handlers.servePageHandler
    events:
      - httpApi:
          path: /{proxy+}
          method: GET

configValidationMode: warn

plugins:
  - serverless-dotenv-plugin
  - serverless-webpack
  - serverless-offline
  - serverless-domain-manager

custom:
  stage: ${env:NODE_ENV}
  domains:
    production: ${env:BASE_DOMAIN}
    staging: sta-${env:BASE_DOMAIN}
    development: dev-${env:BASE_DOMAIN}
  dotenv:
    logging: false
  serverless-offline:
    httpPort: ${env:SLS_OFFLINE_PORT}
    noPrependStageInUrl: false
    resourceRoutes: false
  webpack:
    webpackConfig: webpackConfig.js
    packager: yarn
    includeModules:
      forceExclude:
        - aws-sdk
    keepOutputDirectory: true
  customDomain:
    domainName: ${self:custom.domains.${self:custom.stage}}
    stage: ${env:NODE_ENV}
    endpointType: regional
    apiType: http
    autoDomain: true